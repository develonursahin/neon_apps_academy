import 'package:flutter/material.dart';

class RoutePlanningPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Route Planning'),
      ),
      body: Center(
        child: Text('Rota Planning Page'),
      ),
    );
  }
}
